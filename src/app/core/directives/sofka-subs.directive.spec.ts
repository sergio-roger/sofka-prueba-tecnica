import { SofkaSubsDirective } from './sofka-subs.directive';

describe('SofkaSubsDirective', () => {
  it('should create an instance', () => {
    const directive = new SofkaSubsDirective();
    expect(directive).toBeTruthy();
  });
});
