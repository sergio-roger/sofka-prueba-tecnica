import { SofkaFormDirective } from './sofka-form.directive';

describe('SofkaFormDirective', () => {
  it('should create an instance', () => {
    const directive = new SofkaFormDirective();
    expect(directive).toBeTruthy();
  });
});
